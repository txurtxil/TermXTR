name: termxtr
description: "XTR Terminal — SSH/SFTP client Android"
publish_to: none
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  path_provider: ^2.1.3
  permission_handler: ^11.3.1
  cupertino_icons: ^1.0.8
  xterm: ^4.0.0
  flutter_pty: ^0.4.2
  ssh: ^0.19.0
  sftp: ^0.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
